# Required
SPARK_API_KEY=spk_live_f77dc4b376e29f0debb2be109b57a69a699eeeb7d56ada31

# Optional if you already created a bucket manually
SPARK_BUCKET_ID=1

# Optional server config
PORT=3000

# Optional defaults for bucket creation endpoint
DEFAULT_BUCKET_NAME=sessions
DEFAULT_BUCKET_PUBLIC=false

# MinIO / S3-compatible storage (replaces the old SPARK_API_* vars)
MINIO_ENDPOINT=bucket.sparkdb.pro
MINIO_PORT=443
MINIO_USE_SSL=true
MINIO_ACCESS_KEY=spark
MINIO_SECRET_KEY=hakisolos
MINIO_BUCKET=sessions
